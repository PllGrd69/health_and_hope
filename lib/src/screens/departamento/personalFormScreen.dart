import 'package:flutter/material.dart';


class FormRegisterPersonal extends StatelessWidget {
  const FormRegisterPersonal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
